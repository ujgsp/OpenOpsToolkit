---
# Nginx Role - Main Tasks
# Install and enable Nginx

- name: Install Nginx
  apt:
    name: nginx
    state: present
    update_cache: yes
    cache_valid_time: 3600

- name: Start and enable Nginx
  systemd:
    name: nginx
    state: started
    enabled: yes
